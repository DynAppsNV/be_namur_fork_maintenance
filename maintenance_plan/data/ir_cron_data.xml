<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <data noupdate="1">
        <record id="ir_cron_create_maintenance_requests" model="ir.cron">
            <field name="name">Maintenance Plan : create maintenance requests</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=3, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="model_id" ref="maintenance_plan.model_maintenance_plan"/>
            <field name="state">code</field>
            <field name="code">model.cron_create_maintenance_requests()</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
